feat(duo-chrome): implement history system foundation and integration

Implements Taskmaster task 35 (duo-chrome history/filmstrip feature) by
completing the initial history system setup and integration.

Completed Kiro Spec Tasks:
- Task 1: Create history data structures and core module ✓
- Task 2: Implement history stack operations (partial - navigation methods) ✓
- Task 3: Integrate history capture with parameter change functions ✓

Created Files:
- .kiro/specs/duo-chrome-history-filmstrip/tasks.md
  * Complete implementation plan for history/filmstrip feature
  * 20 tasks covering data structures, UI, persistence, and testing

History System Implementation:
- Created HistoryEntry.js with entry creation and validation
- Created HistoryManager.js with core history operations:
  * captureCurrentState() - captures composition snapshots
  * navigateTo(), navigateBackward(), navigateForward() - navigation
  * Position tracking and bounds checking
  * Maximum entries enforcement (500 entry limit)
  * Truncation on modification (linear history model)
- Property-based tests for history capture completeness and position bounds

Integration with duo-chrome:
- Imported and initialized HistoryManager in duo-chrome.js
- Added debouncing system for rapid parameter changes (300ms delay)
- Integrated history capture hooks into all parameter change functions:
  * adjustImageSize() - debounced capture for rapid size adjustments
  * navigateImage() - immediate capture for discrete navigation
  * navigateImageColor() - immediate capture for color changes
  * exchangeImages() - immediate capture for image swaps
  * cycleBlendMode() - immediate capture for blend mode changes
  * toggleBackgroundColor() - immediate capture for background changes
- Capture initial state after images load
- Capture URL-restored compositions with 'url' source
- Use getter/setter pattern for primitive state values

Technical Details:
- Debouncing prevents excessive history entries during rapid changes
- Immediate capture ensures all meaningful changes are recorded
- State references use getters for primitives to ensure current values
- HistoryManager's isNavigating flag prevents capture during navigation
- Lightweight entries (parameters only, no image data) enable 500+ history

Requirements Satisfied:
- Requirement 1.1: Automatic capture on parameter changes
- Requirement 1.2: Complete composition state capture
- Requirement 1.3: Maximum entries enforcement
- Requirement 3.3, 3.4: Position bounds checking
- Requirement 3.5: Navigation doesn't create entries

Next Steps:
- Task 4: Complete composition state restoration integration
- Task 6: Implement keyboard navigation
- Task 7: Implement localStorage persistence

Taskmaster: task_035
Spec: .kiro/specs/duo-chrome-history-filmstrip/tasks.md
